<Page
    x:Class="TransactionMonitor.Views.RiskLabelsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Метки риска" FontSize="32" FontWeight="Bold"/>
                <TextBlock Text="Управление метками и тегами для транзакций"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            <Button Grid.Column="1" x:Name="AddLabelButton" Content="Новая метка"
                    Click="Add_Click"
                    VerticalAlignment="Center"
                    Style="{StaticResource AccentButtonStyle}"/>
        </Grid>

        <!-- Статистика -->
        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="Всего меток" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TotalText" Text="—" FontSize="32"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="High" FontWeight="SemiBold"/>
                    <TextBlock x:Name="HighText" Text="—" FontSize="32" Foreground="#F44336"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="Medium" FontWeight="SemiBold"/>
                    <TextBlock x:Name="MediumText" Text="—" FontSize="32" Foreground="#FF9800"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="20">
                <StackPanel Spacing="8">
                    <TextBlock Text="Low" FontWeight="SemiBold"/>
                    <TextBlock x:Name="LowText" Text="—" FontSize="32" Foreground="#4CAF50"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Список меток -->
        <ListView x:Name="LabelsList"
                  Grid.Row="2"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="16" ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding IdText}"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <TextBlock Grid.Column="1" Text="{Binding LabelName}"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"/>

                        <TextBlock Grid.Column="2" Text="{Binding Description}"
                                   VerticalAlignment="Center"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"/>

                        <Border Grid.Column="3"
                                Background="{Binding SeverityBackground}"
                                CornerRadius="4" Padding="8,4"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Severity}"
                                       FontSize="12"
                                       Foreground="{Binding SeverityForeground}"/>
                        </Border>

                        <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="4"
                                    VerticalAlignment="Center">
                            <Button Click="Edit_Click" Tag="{Binding}"
                                    Padding="6" Background="Transparent">
                                <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                            </Button>
                            <Button Click="Delete_Click" Tag="{Binding}"
                                    Padding="6" Background="Transparent"
                                    Visibility="{Binding DeleteVisible}">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#F44336"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>