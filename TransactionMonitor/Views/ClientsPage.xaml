<Page
    x:Class="TransactionMonitor.Views.ClientsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="32" RowSpacing="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Spacing="4">
            <TextBlock Text="Клиенты" FontSize="32" FontWeight="Bold"/>
            <TextBlock Text="Управление клиентами системы"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Поиск и экспорт -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox"
             Grid.Column="0"
             PlaceholderText="Поиск по имени, телефону..."
             TextChanged="SearchBox_TextChanged"
             MaxWidth="400"
             HorizontalAlignment="Left"/>
            <TextBlock x:Name="CountText"
               Grid.Column="1"
               VerticalAlignment="Center"
               Margin="16,0"
               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <Button x:Name="ExportButton"
            Grid.Column="2"
            Content="📥 Экспорт CSV"
            Click="Export_Click"/>
        </Grid>

        <!-- Таблица -->
        <Border Grid.Row="2"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Шапка таблицы -->
                <Grid Grid.Row="0" Padding="16,12"
                      Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" TextAlignment="Center" Text="ID" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Padding="4,0,0,0" Text="ФИО" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Padding="26,0,0,0" Text="Телефон" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Padding="3,0,0,0" Text="Email" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Padding="10,0,0,0" Text="Риск-скор" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="5" Padding="5,0,0,0" Text="Статус" FontWeight="SemiBold"/>
                </Grid>

                <!-- Список клиентов -->
                <ListView x:Name="ClientsList"
                      Grid.Row="1"
                      SelectionMode="Single"
                      IsItemClickEnabled="True"
                      ItemClick="ClientsList_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4,8" ColumnSpacing="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Padding="15,0,0,0"
                                           Text="{Binding ClientID}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock Grid.Column="1"
                                           Padding="0,0,0,0"
                                           Text="{Binding FullName}"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Grid.Column="2"
                                           Padding="20,0,0,0"
                                           Text="{Binding Phone}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                                <TextBlock Grid.Column="3"
                                           Padding="0,0,0,0"
                                           Text="{Binding Email}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>

                                <TextBlock Grid.Column="4"
                                           Padding="30,0,0,0"
                                           Text="{Binding RiskScoreFormatted}"
                                           Foreground="{Binding RiskColor}"/>

                                <Border Grid.Column="5"
                                        Background="{Binding StatusBackground}"
                                        CornerRadius="4"
                                        Padding="10,3"
                                        HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding StatusText}"
                                               Foreground="{Binding StatusForeground}"
                                               FontSize="12"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>